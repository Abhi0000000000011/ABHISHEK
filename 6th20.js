let u = document.querySelector("div");
console.dir(u);
console.dir(u.innerText);
// this u.inner text will give output only the elements inside the div



console.dir(u.innerHTML);
// inner.HTML shows every thing inside the element which we have written

console.dir(u.innerText);
console.dir(u.textContent);    //both are diff but inner.html works like text.content


































































































